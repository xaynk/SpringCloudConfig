spring:
  application:
    name: ont-acs
  cloud:
    config:
      # App service pointing to Config-Server directly
      uri: http://${CONFIG_URL:localhost}:8888

management:
  security:
    enabled: false
